/**
 * Create the function placeDisk(row: number, color: 'R'| 'B') => boolean;
 *
 * that places a disk in a Game object and returns a Boolean value wether the player has won the game with that move.
 * Given that the player has won the game when it has 3 consequetive disks either vertically or horizontally.
 *
 * Additionally the game space is infinite.
 * Both X and Y go up to infinity.
 */

// Solution:
// game: { [x: number]: number[] }
